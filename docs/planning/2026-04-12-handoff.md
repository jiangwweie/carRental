# 交接文档 - 租车应用

**日期**: 2026-04-12
**状态**: Sprint 3 补充完成 + 集成测试修复

---

## 今日完成

### 1. 小程序用户端集成测试（33/33 通过）

按用户故事地图逐一测试小程序用户端：

| 用户故事 | 结果 | 说明 |
|---------|------|------|
| US-01 登录 | ✅ | Mock 登录 + 用户信息返回 + 跳转 |
| US-02 浏览车辆 | ✅ | 列表 5 辆车 + 价格筛选 |
| US-03 车辆详情 | ✅ | 详情完整 + 下架 404 |
| US-04 预订下单 | ✅ | 创建 + 后端算价 + 冲突检测 |
| US-05 查看订单 | ✅ | 列表/详情/取消/状态筛选/数据隔离 |
| US-06 用户协议 | ✅ | 协议校验 + 内容 |
| US-18 租期选择 | ✅ | 价格预估 + 价格明细 |
| US-19 订单状态 | ✅ | 4 步进度条 |
| US-21 登录态管理 | ✅ | 无效 token 拒绝 + 无 token 拒绝 + 权限隔离 |
| US-22 取车指引 | ✅ | 地址 + 营业时间 + 提示 |
| US-25 空状态 | ✅ | 空列表处理 |

### 2. 后端 Bug 修复（8 项 P0）

| # | Bug | 修复 | 文件 |
|---|-----|------|------|
| 1 | MyBatis `<if>` 动态 SQL 未生效 | `@Select` 包裹 `<script>` | OrderMapper.java |
| 2 | HTML 实体 `&lt;` 未解码 | 改为 `<` `>` | OrderMapper.java |
| 3 | JacksonTypeHandler 不支持 LocalDate | 注册 JavaTimeModule | MyBatisPlusConfig.java |
| 4 | 数据库缺少 reject_reason 列 | Flyway V3 迁移 | V3__*.sql |
| 5 | 车辆图片 images 为 null | Flyway V4 占位图 | V4__*.sql |
| 6 | JSON 字段不序列化 | `@TableName` 加 autoResultMap | VehicleDO/OrderDO.java |
| 7 | 车辆列表 images 仍为 null | 改用 LambdaQueryWrapper | VehicleRepositoryImpl.java |
| 8 | snake_case vs camelCase | request.js 统一转换 | request.js |

### 3. 技术决策记录

| 决策 | 说明 |
|------|------|
| 前端字段转换策略 | 在 `request.js` 统一 snake_case → camelCase 转换，而非后端修改 DTO |
| 图片占位方案 | SVG base64 嵌入数据库，v1.5 迁移 OSS 时替换 |
| 自定义查询改用 QueryWrapper | `@Select` 自定义 SQL 不走 autoResultMap，改用 MyBatis-Plus QueryWrapper |

---

## 已知问题（不阻塞核心流程）

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| 前端 `useMock=false` 已设置 | P1 | 后端已就绪，字段已适配，需要小程序端实际验证 |
| 订单详情页手机号未脱敏 | P1 | AdminOrderController 直接返回原始手机号 |
| 订阅消息未实现 | P1 | US-07/14 TODO 注释，需小程序上线后接入 |
| Token 无过期管理 | P2 | Mock token 永久有效 |
| PricingView loading 状态 | P2 | 0.5h 待办 |
| VehicleView 价格默认值 | P2 | 0.3h 待办 |

---

## 阻塞项

| 阻塞项 | 影响 | 解决条件 | 负责人 |
|--------|------|---------|--------|
| 微信支付商户号未申请 | T21-T23 | 申请微信支付商户 | 用户 |
| OSS/COS 未开通 | T24 | 开通阿里云 OSS 或腾讯云 COS | 用户 |
| 小程序需上线 | T14/15 | 完成小程序注册和上线 | 用户 |

---

## 下一步优先级

### Sprint 4: 支付闭环（需外部条件）

| # | 任务 | 预估 | 说明 |
|---|------|------|------|
| 1 | T21 微信支付接入 | 3h | 需支付商户号 |
| 2 | T22 小程序支付流程 | 2h | 依赖 T21 |
| 3 | T23 退款流程 | 1.5h | 依赖 T21 |
| 4 | T24 图片迁移到 OSS | 2h | 需 OSS/COS 账号 |
| 5 | T25 完整 PricingEngine | 2h | 需节假日配置完成 |

### P2 技术债务

| # | 任务 | 预估 | 说明 |
|---|------|------|------|
| 6 | T19 PricingView loading | 0.5h | 加载车辆列表时显示 loading |
| 7 | T20 VehicleView 价格默认值 | 0.3h | 新增弹窗价格默认值优化 |

---

## 关键文件位置

```
backend/
├── src/main/java/com/carrental/
│   ├── common/config/
│   │   └── MyBatisPlusConfig.java     # JSR310 注册
│   ├── infrastructure/persistence/
│   │   ├── dataobject/
│   │   │   ├── VehicleDO.java        # autoResultMap = true
│   │   │   └── OrderDO.java          # autoResultMap = true
│   │   ├── mapper/
│   │   │   └── OrderMapper.java      # <script> 包裹动态 SQL
│   │   └── repository/
│   │       └── VehicleRepositoryImpl.java  # QueryWrapper
│   └── controller/
│       └── VehicleController.java    # 车辆列表/详情
├── src/main/resources/db/migration/
│   ├── V3__add_reject_reason_to_orders.sql
│   └── V4__add_vehicle_placeholder_images.sql
frontend-mini/
├── src/api/
│   └── request.js                    # snake_case → camelCase 转换
└── src/pages/
    ├── index/index.vue               # 首页 + 价格筛选
    ├── orders/orders.vue             # 订单列表 + 全部 Tab
    └── me/me.vue                     # 个人中心 + 退出登录
```

---

## 后端运行方式

```bash
# MySQL 已运行（Docker）
docker ps  # 确认 car-rental-mysql 运行

# 启动后端（端口 8081）
cd backend && mvn spring-boot:run
```

---

*交接时间: 2026-04-12*
